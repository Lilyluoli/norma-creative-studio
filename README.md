# NØRMA — 独立创意工作室

这是一个纯静态网站，仅使用原生 HTML、CSS 和 JavaScript，不依赖 React、Next.js、Vite 或其他前端框架。

## 本地预览

直接用浏览器打开 `index.html`，或使用任意静态文件服务器预览。

## GitHub Pages

仓库已包含 `.github/workflows/pages.yml`。推送到 `main` 后，GitHub Actions 会自动发布根目录静态文件。
